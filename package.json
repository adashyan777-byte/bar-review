{
  "name": "bar-review",
  "version": "1.0.0",
  "private": true,
  "description": "Тести бару — вправа на ревʼю коду",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
